[package]
name = "machina-compat"
version = "0.1.0"
edition = "2021"
description = "Compatibility-mode host proxy services for Machina"
license = "GPL-2.0"

[dependencies]
libc = "0.2"
machina-mode = { path = "../machina-mode" }

[lib]
name = "machina_compat"
path = "src/lib.rs"
